# === Proyecto ===
PROJECT_NAME=magento2-boilerplate

# === PHP ===
# Magento Open Source 2.4.8 soporta 8.3 y 8.4. Usamos 8.3 por estabilidad
# con extensiones de terceros (muchas aun no certifican 8.4/8.5).
PHP_VERSION=8.3

# === Puertos expuestos al host ===
NGINX_PORT=8080
DB_PORT=3306
OPENSEARCH_PORT=9200
REDIS_PORT=6379
MAILPIT_UI_PORT=8025
MAILPIT_SMTP_PORT=1025

# === Base de datos ===
DB_ROOT_PASSWORD=magento_root
DB_NAME=magento
DB_USER=magento
DB_PASSWORD=magento

# === Certificado corporativo (Netskope u otro proxy con SSL inspection) ===
# Si NO estás detrás de un proxy corporativo, deja esto vacío.
# Si sí lo estás (como en tu red de Truper), exporta el certificado raíz
# de Netskope a docker/php/certs/corporate-ca.crt y pon aquí el nombre del archivo.
CORPORATE_CA_CERT=
